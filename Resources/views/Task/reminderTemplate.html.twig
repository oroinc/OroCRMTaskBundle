<script type="text/html" class="reminder_templates" data-identifier="task_template">
<% let safeSubject = !url ? _.escape(subject) : `<a class="hash-navigation-link" href="${_.escape(url)}">${_.escape(subject)}</a>`; %>
<%= _.__('oro.task.reminder_template.due_message', {
    subject: safeSubject,
    expireAt: _.escape(expireAt)
}) %>
</script>
