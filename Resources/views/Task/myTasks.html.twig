{% extends '@OroUI/actions/index.html.twig' %}

{% set name = app.user|oro_format_name|default('N/A'|trans) %}
{% oro_title_set({params : {"%username%": name }}) %}

{% set pageTitle = 'oro.task.entity_plural_label'|trans %}
{% set gridName = 'user-tasks-grid' %}
{# grid params #}
{% set params = {'userId': app.user.id} %}

{% block breadcrumb %}
    {% set breadcrumbs = [
        {'label': 'oro.task.menu.my_tasks'|trans }
    ] %}
    {% include '@OroNavigation/Menu/breadcrumbs.html.twig' %}
{% endblock breadcrumb %}
