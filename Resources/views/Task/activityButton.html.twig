{{ UI.clientButton({
    'dataUrl': path('oro_task_create', oro_action_params(entity, 'activity')),
    'aCss': 'no-hash',
    'iCss': 'icon-tasks',
    'dataId': entity.id,
    'label': 'oro.task.add_entity'|trans,
    'title': 'oro.task.add_entity.title'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : true,
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'task-dialog',
            'dialogOptions' : {
                'title' : 'oro.task.add_entity'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 1000
            }
        }
    }
}) }}
